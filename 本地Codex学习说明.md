# 本地 Codex 即时学习模式

## 怎么启动

双击项目目录里的 `启动本地Codex学习站.command`。

启动器会自动完成三件事：

1. 检查本机 Codex 是否已通过 ChatGPT 登录。
2. 启动课程网页和只监听本机的 Codex 学习桥。
3. 打开 `http://localhost:3000/courses/python-framework`。

请保留启动器打开的终端窗口。关闭窗口或按 `Control+C` 后，本地对话功能会停止。

## 怎么使用

进入任一关的“动手练”，先在代码框写答案。看到“本机 Codex 已连接”后，可以：

- 点击“立即批改当前答案”；
- 点击“只给我一点提示”；
- 在聊天框继续问“为什么错”“这个函数是什么意思”或“再检查一次”。

批改结果会写回当前浏览器的课程进度。线上站点仍保留异步队列作为备用。

## 安全边界

- 学习桥只监听 `127.0.0.1`，不会对局域网或公网开放。
- 只接受本地课程页面的请求。
- Codex 以非交互只读沙箱运行，不允许修改课程或其他本地文件。
- 不需要 API Key；调用使用本机当前的 Codex 登录。
